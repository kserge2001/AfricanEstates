# Environment variables for the Docker Compose setup
NODE_ENV=development

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=african_estates

# This will be automatically constructed in docker-compose.yml, but is shown here as reference:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/african_estates

